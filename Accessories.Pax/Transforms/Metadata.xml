<metadata>
    
  <!--
  remove everything and add back what we want. Too many errors to fix
  -->

  <remove-node path="/api/package[starts-with(@name, 'com.pax.poslink')]" />
  <remove-node path="/api/package[starts-with(@name, 'com.pax.gl')]" />

</metadata>
